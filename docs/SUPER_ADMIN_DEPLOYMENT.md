# Super Admin deployment

Run `docker compose up --build`. Laravel is exposed on 8000 and Super Admin on 3000. Use `SUPER_ADMIN_*` only for local seeded access; production accounts require secure provisioning.
