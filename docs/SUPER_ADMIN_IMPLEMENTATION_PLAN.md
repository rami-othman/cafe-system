# Super Admin implementation plan

The first vertical slice introduces isolated session authentication, platform RBAC, audit logging, plans, subscriptions, tenant onboarding, dashboard metrics, tenant directory, and a Next.js web shell. Existing Flutter `/api/v1` routes remain untouched.

Next slices: branch/user/subscription management APIs, announcements, health and settings APIs, full Arabic RTL translations, invitations, and export jobs.
