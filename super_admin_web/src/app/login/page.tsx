'use client';
import { useRouter } from 'next/navigation';import { FormEvent,useState } from 'react';import { api,csrf } from '@/lib/api/client';
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);const form=new FormData(e.currentTarget);try{await csrf();await api('/auth/login',{method:'POST',body:JSON.stringify({email:form.get('email'),password:form.get('password')})});router.replace('/dashboard')}catch{setError('تعذر تسجيل الدخول بهذه البيانات.')}finally{setLoading(false)}}return <main className="login"><h1>Cafe 6:18 Platform</h1><p className="muted">دخول آمن لإدارة المنصة</p><form onSubmit={submit}><input name="email" type="email" placeholder="البريد الإلكتروني" required/><input name="password" type="password" placeholder="كلمة المرور" required/><button className="button" disabled={loading}>{loading?'جارٍ الدخول…':'تسجيل الدخول'}</button>{error&&<p className="error">{error}</p>}</form></main>}
