enum ProductType { simple, variant, combo }

enum ProductStatus { active, inactive, hidden, soldOut, draft }

enum ModifierSelectionType { single, multiple }

enum StockStatus { inStock, lowStock, unavailable }

enum ChannelType { dineIn, takeaway, delivery }
